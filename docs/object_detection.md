# Object Detection

The package supports a dependency-free mock adapter, optional user-supplied Ultralytics-compatible weights, and Transformers zero-shot detection. Source predictions and fused boxes are retained separately.
